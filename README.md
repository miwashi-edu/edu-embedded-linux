# edu-embedded-linux

> Same as level-5, but in C. c-tooling added to ws.

## If you ddon't have c-tooling

```bash
cd ws
cd edu-embedded-linux
git pull
```
> All working, it should add c-tooling in your docker machine under ~/ws/
